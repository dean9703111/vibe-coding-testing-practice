> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、狀態邏輯、驗證權限...

---

## [x] 【前端元素】顯示完整的 AdminPage UI
**範例輸入**：Mock `useAuth` 提供有效的 `user` 角色資訊，正常渲染該元件。
**期待輸出**：畫面成功顯示「← 返回」、「🛠️ 管理後台」標題、角色徽章、登出按鈕與「管理員專屬頁面」的特徵說明卡片。

---

## [x] 【狀態邏輯】點擊返回連結導向回 Dashboard
**範例輸入**：點擊「← 返回」的連結按鈕。
**期待輸出**：由於使用 `<Link to="/dashboard">`，點擊後應能正確往由 Router 分配至 `/dashboard` 路徑。

---

## [x] 【狀態邏輯】點擊登出按鈕會呼叫 logout 與轉址
**範例輸入**：點擊畫面右上方的「登出」按鈕。
**期待輸出**：呼叫 `useAuth` 的 `logout` 函式，並觸發 `navigate('/login', { replace: true, state: null })`。

---

## [x] 【狀態邏輯】使用者為 admin 時徽章顯示管理員
**範例輸入**：Mock `useAuth` 回傳 `user` 為 `{ role: 'admin' }`。
**期待輸出**：頂端導覽列的角色徽章顯示為「管理員」。

---

## [ ] 【狀態邏輯】若使用者為一般用戶時徽章顯示一般用戶
**範例輸入**：Mock `useAuth` 回傳 `user` 為 `{ role: 'user' }` 雖然實務上有 router 阻擋，但負責單獨組件測試。
**期待輸出**：頂端導覽列的角色徽章顯示為「一般用戶」。
